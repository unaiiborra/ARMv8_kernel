[package]
name = "stl"
version = "0.1.0"
edition = "2024"

[dependencies]


[lib]
name = "stl"
crate-type = ["staticlib", "rlib"]
test = false
doctest = false
bench = false

[profile.release]
panic = "abort"

[build-dependencies]
glob = "0.3.3"
cc = "1.2.62"
dotenvy = "0.15.7"
